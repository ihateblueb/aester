{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host 9080",
    "generate": "nuxt generate",
    "preview": "nuxt preview --port=3001",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.7.0"
  },
  "dependencies": {
    "feather-icons": "^4.29.1",
    "vue": "^3.3.4",
    "vue-cookie": "^1.1.4",
    "vue-feather": "^2.0.0",
    "vue-router": "^4.1.6"
  }
}
